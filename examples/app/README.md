# Todo Demo App

This demo is written only in Point source.

Run the CLI entrypoint:

```bash
bun packages/point/src/cli.ts run examples/app/todo.point
```

Build generated TypeScript:

```bash
bun packages/point/src/cli.ts build-ts examples/app/todo.point generated/todo.ts
```
