module MaybeNarrow

record Contact
  email: Text
  city: Text

calculation primary email
  input contact: Maybe<Contact>
  output value: Text
  when contact present return contact.email
  when contact is none return "no-email"
  return "no-email"

rule profile score
  input contact: Maybe<Contact>
  input nickname: Maybe<Text>
  output score: Int
  score starts at 0
  add 10 when contact present
  when nickname is none return score
  add 1 to score
  return score

label contact summary
  input contact: Maybe<Contact>
  output Text
  when contact present return contact.city
  when contact is none return "missing contact"
  otherwise return "missing contact"

view profile banner
  input contact: Maybe<Contact>
  when contact present render contact.email
  when contact is none render "Add a contact email"
  render "Profile ready"
