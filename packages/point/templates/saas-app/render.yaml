services:
  - type: web
    name: point-full-stack-app
    runtime: bun
    buildCommand: bun install && bun run build
    startCommand: bun run serve
    envVars:
      - key: PORT
        value: "10000"
