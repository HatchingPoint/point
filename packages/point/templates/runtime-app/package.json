{
  "name": "{{APP_NAME}}",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "point check src/app.point",
    "run": "point run src/app.point smoke",
    "test": "point test tests/score.test.point",
    "dev": "point dev src/app.point",
    "serve": "point serve src/app.point"
  },
  "devDependencies": {
    "@hatchingpoint/point": "^0.2.0"
  }
}
