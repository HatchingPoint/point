# Generated by Point. Do not edit directly.
# Point module: Money

from __future__ import annotations

import sys
from pathlib import Path as _PointPath
_point_here = _PointPath(__file__).resolve()
_point_std_candidates = [_point_here.parents[1] / "packages" / "point" / "python_std"]
for _point_parent in _point_here.parents:
    _point_std_candidates.append(_point_parent / "node_modules" / "@hatchingpoint" / "point" / "python_std")
_point_std_root = next((candidate for candidate in _point_std_candidates if candidate.is_dir()), None)
if _point_std_root is not None and str(_point_std_root) not in sys.path:
    sys.path.insert(0, str(_point_std_root))

from typing import TypedDict

from point_std.money import formatCentsUsd as formatCentsUsdRaw

from point_std.text import textFromInt as textFromIntRaw

class Money(TypedDict):
    amountCents: int
    currency: str

def moneyFromCentsMoney(amountCents: int, currency: str) -> Money:
    return {"amountCents": amountCents, "currency": currency}

def addMoneyTotal(left: Money, right: Money) -> Money:
    return {"amountCents": (left["amountCents"] + right["amountCents"]), "currency": left["currency"]}

def moneyDisplay(money: Money) -> str:
    if money["currency"] == "USD":
        return formatCentsUsdRaw(money["amountCents"])
    return ((textFromIntRaw(money["amountCents"]) + " ") + money["currency"])
