# Generated by Point. Do not edit directly.
# Point module: StdAuth

from __future__ import annotations

import sys
from pathlib import Path as _PointPath
_point_here = _PointPath(__file__).resolve()
_point_std_candidates = [_point_here.parents[1] / "packages" / "point" / "python_std"]
for _point_parent in _point_here.parents:
    _point_std_candidates.append(_point_parent / "node_modules" / "@hatchingpoint" / "point" / "python_std")
_point_std_root = next((candidate for candidate in _point_std_candidates if candidate.is_dir()), None)
if _point_std_root is not None and str(_point_std_root) not in sys.path:
    sys.path.insert(0, str(_point_std_root))

from point_std.auth import authBearerToken as bearerTokenFromAuthorization

from point_std.auth import authJwtOk as jwtAuthOk

from point_std.auth import authSignJwt as signAuthJwtRaw

from point_std.auth import authVerifyJwt as verifyJwtFromAuthorization

from point_std.auth import authUnauthorizedJson as unauthorizedResponseRaw

def bearerToken(authorization: str) -> str:
    return bearerTokenFromAuthorization(authorization)

def authOk(authorization: str, secret: str) -> bool:
    return jwtAuthOk(authorization, secret)

def signAuthJwtToken(payload: str, secret: str) -> str:
    return signAuthJwtRaw(payload, secret)

def verifyAuthJwtPayload(authorization: str, secret: str) -> str | dict[str, str]:
    return verifyJwtFromAuthorization(authorization, secret)

def authUnauthorizedBody() -> str:
    return unauthorizedResponseRaw()
