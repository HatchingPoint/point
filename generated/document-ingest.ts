// Generated by Point. Do not edit directly.
// Point module: DocumentIngest
function pointIsError(value: unknown): value is { message: string } {
  return typeof value === "object" && value !== null && "message" in value && typeof (value as { message?: unknown }).message === "string";
}

function pointPipelineNow(): number {
  return Date.now();
}

function pointPipelineShouldLog(log: PointPipelineLog<PointPipelineEventBase> | null | undefined): boolean {
  return log != null;
}

function pointPipelineEmitLog(
  log: PointPipelineLog<PointPipelineEventBase> | null | undefined,
  pipeline: string,
  step: string,
  phase: PointPipelineEventBase["phase"],
  ok?: boolean,
  error?: { message: string },
): void {
  if (!pointPipelineShouldLog(log)) return;
  const event: PointPipelineEventBase = {
    schemaVersion: "point.pipeline.event.v1",
    pipeline,
    step,
    phase,
    at: pointPipelineNow(),
    ...(ok === true ? { ok: true as const } : {}),
    ...(ok === false ? { ok: false as const, error: error ?? { message: "" } } : {}),
  };
  log!(event);
}

export type PointPipelineLog<T extends PointPipelineEventBase> = (event: T) => void;

export interface PointPipelineEventBase {
  schemaVersion: "point.pipeline.event.v1";
  pipeline: string;
  step: string;
  phase: "start" | "complete" | "failure";
  at: number;
}

export function pointPipelineEventJson(event: PointPipelineEventBase): string {
  return JSON.stringify(event);
}

export function pointPipelineEventsJson(events: readonly PointPipelineEventBase[]): string {
  return JSON.stringify(events);
}

export type documentIngestPipelineEvent =
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "fetched"; phase: "start"; at: number }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "fetched"; phase: "complete"; at: number; ok: true }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "fetched"; phase: "failure"; at: number; ok: false; error: { message: string } }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "parsed"; phase: "start"; at: number }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "parsed"; phase: "complete"; at: number; ok: true }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "parsed"; phase: "failure"; at: number; ok: false; error: { message: string } }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "classified"; phase: "start"; at: number }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "classified"; phase: "complete"; at: number; ok: true }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "classified"; phase: "failure"; at: number; ok: false; error: { message: string } }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "stored"; phase: "start"; at: number }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "stored"; phase: "complete"; at: number; ok: true }
  | { schemaVersion: "point.pipeline.event.v1"; pipeline: "document ingest"; step: "stored"; phase: "failure"; at: number; ok: false; error: { message: string } }
;


export function allowedUrlPolicy(url: string): boolean {
  return (url != "");
}

export async function fetchDocumentBody(url: string): Promise<string | { message: string }> {
  return url;
}

export async function parseDocumentParsed(body: string | { message: string }): Promise<string | { message: string }> {
  return body;
}

export async function classifyDocumentCategory(parsed: string | { message: string }): Promise<string | { message: string }> {
  return "general";
}

export async function storeDocumentPath(documentId: string | { message: string }, category: string | { message: string }): Promise<string | { message: string }> {
  return documentId;
}

export async function documentIngestPipeline(url: string, __pointPipelineLog?: PointPipelineLog<documentIngestPipelineEvent> | null): Promise<string | { message: string }> {
  pointPipelineEmitLog(__pointPipelineLog, "document ingest", "fetched", "start");
  if (allowedUrlPolicy(url) == false) {
    return { message: "Fetch failed" };
  }
  let __pointStepDone: boolean = false;
  let fetched: string | { message: string } = { message: "" };
  for (const __pointAttempt of [0, 1]) {
    if (__pointStepDone == false) {
      fetched = await fetchDocumentBody(url);
      if (pointIsError(fetched) == false) {
        __pointStepDone = true;
      }
      else {
        if (__pointAttempt == 1) {
          return { message: "Fetch failed" };
        }
      }
    }
  }
  if (pointIsError(fetched)) {
    pointPipelineEmitLog(__pointPipelineLog, "document ingest", "fetched", "failure", false, fetched);
  }
  else {
    pointPipelineEmitLog(__pointPipelineLog, "document ingest", "fetched", "complete", true);
  }
  pointPipelineEmitLog(__pointPipelineLog, "document ingest", "parsed", "start");
  const parsed: string | { message: string } = await parseDocumentParsed(fetched);
  if (pointIsError(parsed)) {
    pointPipelineEmitLog(__pointPipelineLog, "document ingest", "parsed", "failure", false, parsed);
  }
  else {
    pointPipelineEmitLog(__pointPipelineLog, "document ingest", "parsed", "complete", true);
  }
  pointPipelineEmitLog(__pointPipelineLog, "document ingest", "classified", "start");
  const classified: string | { message: string } = await classifyDocumentCategory(parsed);
  if (pointIsError(classified)) {
    pointPipelineEmitLog(__pointPipelineLog, "document ingest", "classified", "failure", false, classified);
  }
  else {
    pointPipelineEmitLog(__pointPipelineLog, "document ingest", "classified", "complete", true);
  }
  pointPipelineEmitLog(__pointPipelineLog, "document ingest", "stored", "start");
  const stored: string | { message: string } = await storeDocumentPath(parsed, classified);
  if (pointIsError(stored)) {
    pointPipelineEmitLog(__pointPipelineLog, "document ingest", "stored", "failure", false, stored);
  }
  else {
    pointPipelineEmitLog(__pointPipelineLog, "document ingest", "stored", "complete", true);
  }
  return stored;
}
