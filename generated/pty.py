# Generated by Point. Do not edit directly.
# Point module: StdPty

from __future__ import annotations

import sys
from pathlib import Path as _PointPath
_point_here = _PointPath(__file__).resolve()
_point_std_candidates = [_point_here.parents[1] / "packages" / "point" / "python_std"]
for _point_parent in _point_here.parents:
    _point_std_candidates.append(_point_parent / "node_modules" / "@hatchingpoint" / "point" / "python_std")
_point_std_root = next((candidate for candidate in _point_std_candidates if candidate.is_dir()), None)
if _point_std_root is not None and str(_point_std_root) not in sys.path:
    sys.path.insert(0, str(_point_std_root))

from typing import TypedDict

class PtyHandle(TypedDict):
    id: str

class PtyOutput(TypedDict):
    stderr: str
    exitCode: int

from point_std.pty import ptySpawn as spawnRaw

from point_std.pty import ptyWrite as writeRaw

from point_std.pty import ptyStreamLines as streamLinesRaw

async def spawnPtyHandle(command: str, args: list[str], env: list[str]) -> PtyHandle | dict[str, str]:
    return spawnRaw(command, args, env)

async def writePtyStdinResult(handle: PtyHandle, chunk: str) -> None | dict[str, str]:
    return writeRaw(handle, chunk)

async def streamLinesFromPtyLine(handle: PtyHandle) -> str:
    async for __point_line in streamLinesRaw(handle):
        yield __point_line
