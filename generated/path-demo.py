# Generated by Point. Do not edit directly.
# Point module: PathDemo

from __future__ import annotations

import sys
from pathlib import Path as _PointPath
_point_here = _PointPath(__file__).resolve()
_point_std_candidates = [_point_here.parents[1] / "packages" / "point" / "python_std"]
for _point_parent in _point_here.parents:
    _point_std_candidates.append(_point_parent / "node_modules" / "@hatchingpoint" / "point" / "python_std")
_point_std_root = next((candidate for candidate in _point_std_candidates if candidate.is_dir()), None)
if _point_std_root is not None and str(_point_std_root) not in sys.path:
    sys.path.insert(0, str(_point_std_root))

from path import joinPaths, pathBasename, pathDirname, pathExtname, resolvePath, pathIsAbsolute

def configFilePath(appRoot: str, configSegment: str) -> str:
    return joinPaths(appRoot, configSegment)

def logFileName(filePath: str) -> str:
    return pathBasename(filePath)

def logDirectoryDir(filePath: str) -> str:
    return pathDirname(filePath)

def backupExtensionExt(filePath: str) -> str:
    return pathExtname(filePath)

def absoluteConfigPathResolved(relativePath: str) -> str:
    return resolvePath(relativePath)

def usesAbsolutePathAbsolute(filePath: str) -> bool:
    return pathIsAbsolute(filePath)

async def pathDemoConfigPath(appRoot: str, configSegment: str) -> str:
    return configFilePath(appRoot, configSegment)
