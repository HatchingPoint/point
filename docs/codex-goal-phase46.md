# Codex goals — Phase 46 (real SaaS path)

**Plan:** [phase46-plan.md](./phase46-plan.md)

---

## Wave 1 (parallel — all four)

### Track A — SQL-backed members (P46-1)
```
/goal Execute docs/phase46-plan.md P46-1: sqlJsonRowsList std helper, saas-app fetch members from query member rows, remove sample members from fetch path. Verify check. Do NOT commit unless user asked.
```

### Track B — Build inlining (P46-2)
```
/goal Execute docs/phase46-plan.md P46-2: point build-js uses programWithDependencyDeclarations; test saas-app build is self-contained. Do NOT commit unless user asked.
```

### Track C — E2E integration (P46-3)
```
/goal Execute docs/phase46-plan.md P46-3: saas integration test init db + point build + serve + GET members from DB rows. Do NOT commit unless user asked.
```

### Track D — Docs + pilot (P46-4)
```
/goal Execute docs/phase46-plan.md P46-4: phase46 docs, roadmap, external pilot checklist update. Do NOT commit unless user asked.
```

---

## Wave 2 — Integrator
```
/goal Phase 46 integrator: bun run ci. CHANGELOG v0.1.38. Bump versions. Commit tag v0.1.38 when user asked.
```
