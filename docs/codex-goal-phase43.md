# Codex goals — Phase 43 (parallel burst)

**Plan:** [phase43-plan.md](./phase43-plan.md)

---

## Wave 1 (parallel — all five)

### Track A — std/auth capability (P43-1)
```
/goal Execute docs/phase43-plan.md P43-1: std/auth.point, TS + Python runtime, 15th capability registry entry, auth-demo example, std-auth tests. Append codex-progress. Do NOT commit unless user asked.
```

### Track B — LSP repair enrichment (P43-2)
```
/goal Execute docs/phase43-plan.md P43-2: sortDiagnosticsForRepairPlan, [repair N/M] prefix, relatedRefs in LSP messages + relatedInformation. Append codex-progress. Do NOT commit unless user asked.
```

### Track C — point demo + repair alias (P43-3)
```
/goal Execute docs/phase43-plan.md P43-3: point demo [file], point repair alias, demo.ts with module graph merge, phase43-burst tests. Append codex-progress. Do NOT commit unless user asked.
```

### Track D — Agent repair fixture (P43-4)
```
/goal Execute docs/phase43-plan.md P43-4: auth-bearer-broken/fixed fixtures using auth ok + auth unauthorized body. Append codex-progress. Do NOT commit unless user asked.
```

### Track E — Docs (P43-5)
```
/goal Execute docs/phase43-plan.md P43-5: capabilities.md auth row, golden-app-demo point demo, README, product-map, CLI ref. Append codex-progress. Do NOT commit unless user asked.
```

---

## Wave 2 — Integrator
```
/goal Phase 43 integrator: bun run ci. Mark phase43-plan done. CHANGELOG v0.1.35. Bump versions. Commit tag v0.1.35 when user asked.
```
